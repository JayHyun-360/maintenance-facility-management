# OAUTH FIX - QUICK REFERENCE CARD

## 🎯 THE PROBLEM (1 sentence)
Your database migrations created conflicting `id` vs `user_id` columns, causing the OAuth trigger to fail silently.

## ✅ THE SOLUTION (3 steps)

### 1️⃣ TEST LOCALLY (5 min)
```bash
supabase db reset
npm run dev
# Test OAuth at http://localhost:3000/login
```

### 2️⃣ DEPLOY CODE (5 min)
```bash
git add -A
git commit -m "Fix OAuth schema mismatch"
git push origin main
# Vercel auto-deploys
```

### 3️⃣ DEPLOY SCHEMA (3 min)
```bash
supabase db push
# Enter database password when prompted
```

---

## 📋 VERIFICATION (5 min)

### Test in Production
```
https://maintenance-facility-management.vercel.app/login
→ Click "Sign in with Google"
→ Should redirect to dashboard (NOT error page)
✅ SUCCESS if: Profile created + redirected
```

### Verify Schema
```sql
-- Run in Supabase SQL Editor
SELECT column_name FROM information_schema.columns
WHERE table_name = 'profiles' ORDER BY ordinal_position;

-- Should show: id, full_name, database_role, visual_role, ...
-- Should NOT show: user_id
```

---

## 📁 FILES INVOLVED

| File | Change | Why |
|------|--------|-----|
| `supabase/migrations/20260225000000_consolidate_schema_fix.sql` | ✨ NEW | Fixes schema |
| `src/app/auth/callback/route.ts` | 🔧 FIXED | Use `id` not `user_id` |
| `src/app/auth/callback/AuthCallbackClient.tsx` | 🔧 FIXED | Use `id` not `user_id` |

---

## 🆘 IF IT STILL FAILS

```sql
-- 1. Get recent user
SELECT id FROM auth.users ORDER BY created_at DESC LIMIT 1;

-- 2. Run diagnostic (use ID from above)
SELECT * FROM public.debug_oauth_issue('USER_ID_HERE');

-- 3. Check logs
-- Supabase Dashboard → Logs → Filter "error"
```

---

## ⏱️ TIME ESTIMATE
- Local testing: 5 min
- Code deploy: 5 min  
- Schema deploy: 3 min
- Verification: 5 min
- **Total: ~20 minutes**

---

## 📚 DOCUMENTATION
- **QUICK_FIX_STEPS.md** ← Detailed steps
- **VISUAL_SUMMARY.md** ← See the problem & solution
- **OAUTH_FIX_REPORT.md** ← Complete analysis
- **OAUTH_ERROR_DIAGNOSIS.md** ← Deep debugging

---

## ✅ SUCCESS INDICATORS
- ✓ OAuth completes without error
- ✓ User redirected to profile creation or dashboard
- ✓ New profile appears in Supabase `profiles` table
- ✓ No "database error" or "column" messages in logs

---

**Status**: ✅ READY TO DEPLOY - All fixes prepared and ready for production
